[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Optional: set environment variables here for local/netlify-global overrides
# You still should set secrets (API keys, client IDs) in Netlify UI, not here.